You are doing post-editing of Python stub modules generated by a script.

The directory `build/Kodistubs` contains generated Python stub modules for Kodi Python API with dostrings on various levels:
module, functions, classes, methods. This directory is not managed by Git and should not be committed to the repository.
The docstrings use reStructuredTest formatting that is compatible with Python Sphinx documentation generator.
The docstrings may contain errors in reStructuredText formatting. Your task is to review and fix all the dostrings
so that they include only valid reStructuredText that can be consumed by Sphinx to generate HTML documentation.

Specific instructions:
- Ignore files located in directories other than `build/Kodistubs`. They are not relevant to this task.
- Do not remove any existing docstrings.
- The maximum line length is 100 characters for text and 120 characters for tables.
- Observe PEP-8 guidelines for Python code formatting except for the line length limit as specified above.
- Observe PEP-257 guidelines for Python docstrings formatting except for the line length limit as specified above.
- Observe Sphinx documentation guidelines for reStructuredText formatting.
- Fix unresolved references to classes located in other stub modules being edited but avoid circular imports.
  Use `if TYPE_CHECKING:` condition guard to import unresolved classes from other modules where they are defined.
- Remove unused imports from `typing` module.
- Correctly continue text to the next lines in paragraphs. Wrap long lines that exceed the max line length without
  breaking reStructuredText formatting.
- `Example::` blocks introduce code usage examples as reStructuredText literal blocks. Fix line lengths in literal blocks without
  breaking Python syntax.
- `@python_v<version_number>` patterns do not have any special syntax meaning. They mean that some feature was introduced
  in a specific Kodi Python API version. Treat them as plain text but enclose them in reStructuredText
  bold formatting. Comments introduced by the `@python_v<version_number>` patterns should be placed in its own paragraphs that should
  start with `@python_v<version_number>`. 
- In case of several `@python_v<version_number>` occurrences within a single paragraph split this paragraph into several ones,
  each starting with the respective `@python_v<version_number>` text.
- The descriptions for function/method parameters, return types and exceptions should be separated from the rest 
  of the dostring text by a blank line, according to reStructuredText formatting.
- Fix inline literal elements marked with single backticks `` ` ``. They should be enclosed in double backticks ``` `` ```
  according to reStructuredText formatting rules.
- Remove all possible style markers, e.g. bold `**`, from all reStructuredText inline literal elements.
- Class `xbmcvfs.File` is a context manager. Add missing methods of Python context manager protocol.
- Fix reStructuredText tables formatting, if necessary, according to the specs: 
  ![Tables in reStructuredText -> Simple Tables](https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html#simple-tables)
- Do not break and wrap symbolic constant names written in `ALL_CAPS` in table cells, extend table cells if necessary.
- Do not split table borders marked with `=` characters into multiple lines. Adjust table border lengths and table cell
  widths if necessary. Correctly position internal vertical table borders markers represented as gaps in the rows 
  of `=` characters according to reStructuredText table formatting.
- Text inside table cells should be aligned with left cell borders. Do not insert extra spaces at the beginning of 
  the left-most table cells.
- The maximum line length is 120 characters. Fix table widths, if necessary, by wrapping long lines in table cells
  but do not break reStructuredText formatting. The example is below:

Before:
```
======================================================= =========================================== ==================== ========================================================
Lorem ipsum dolor sit amet, consectetur adipiscingelit. Proin eu elit vehicula, suscipit nisl consequat, tristique nibh. Curabitur commodo lorem ut ex ornare varius eu id ipsum.
======================================================= ================================================================ ========================================================
```
After:
```
====================================== ======================================= =========================================
Lorem ipsum dolor sit amet,            Proin eu elit vehicula, suscipit nisl   Curabitur commodo lorem ut ex ornare
consectetur adipiscingelit.            consequat, tristique nibh.              varius eu id ipsum.
====================================== ======================================= =========================================
```
